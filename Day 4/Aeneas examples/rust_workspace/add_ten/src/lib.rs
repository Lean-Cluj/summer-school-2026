pub fn add_ten(x: u32) -> u32 {
    x + 10
}
/*
pub fn calculate_score() -> u32 {
    let base = 5;
    add_ten(base)
}

pub fn bad_score(user_input: u32) -> u32 {
    add_ten(user_input)
}

pub fn good_score(user_input: u32) -> Option<u32> {
    if user_input <= u32::MAX - 10 {
        Some(add_ten(user_input))
    } else {
        None
    }
}
*/
    
