name = "day2"
version = "0.1.0"

# The material consists of standalone `.lean` files under `logic/` and
# `tactics/`, the appendix included. They are not compiled into a library:
# each file is opened and stepped through in the editor.
# `scripts/check.sh` elaborates all of them in one go.

[[require]]
name = "mathlib"
git = "https://github.com/leanprover-community/mathlib4.git"
rev = "v4.33.0"
